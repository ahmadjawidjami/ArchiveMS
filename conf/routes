# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()
GET		/archive					controllers.Application.renderArchivePage()
GET		/category					controllers.Application.renderCategory()
POST	/category/add				controllers.CategoryCreator.createCategory()
POST	/category/remove			controllers.CategoryCreator.deleteCategory()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
